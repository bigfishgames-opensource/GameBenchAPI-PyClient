dist: xenial   # required for Python >= 3.7

language: python

sudo: true

python:
  - "3.7"

install:
  - pip install pipenv
  - pipenv install

script: python -m unittest gamebench_api_client/tests/test_unit/test_global_settings.py
